// CTA banner — the Asset Guard claim, linking to the product page.
export default function CtaBanner() {
  return (
    <section className="cta">
      <h2>Every asset has a number. Every number has a next step.</h2>
      <p>
        That is the thinking behind Asset Guard — and the same mindset drives
        everything I build: make the invisible visible, then act on it.
      </p>
      <a className="btn" href="/asset-guard">Explore Asset Guard</a>
    </section>
  );
}