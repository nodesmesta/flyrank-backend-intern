import type { NextConfig } from "next";

// BE-09 site — the React Flow editor. It talks to the local Express API
// (localhost:3000), so it is served by `next dev` rather than a static export.
const nextConfig: NextConfig = {};

export default nextConfig;
